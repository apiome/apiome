{
  "name": "apiome-rest",
  "version": "1.37.0",
  "private": true,
  "description": "Trigger file for Apiome REST Docker publish workflow; app is Python (pyproject.toml).",
  "scripts": {
    "dev": "./run.sh"
  }
}
